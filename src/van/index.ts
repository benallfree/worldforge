import van, { type ChildDom, type Props, type State } from './van-0.12.3.debug'

export const { button, div, a, p, pre, br, input, img, sup, h1, textarea } = van.tags
export const { state, bind } = van

export { ChildDom, Props, State, van }
